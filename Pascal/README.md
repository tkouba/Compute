# Pascal version

Pascal have two different mathematical operation:
* `div` - Integer division
* `/` - Division

Results are different for `div` and `/`. The result of `div` is integer expression and the result `/` is real expression. 
I use `div` as a copy of original C# code.